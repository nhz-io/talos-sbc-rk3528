---
kind: pkgfile.Build
spec:
  targets:
    - sbc-rk3528
  extraBuildArgs:
    - PKGS_PREFIX
    - PKGS
  makefile:
    extraVariables:
      - name: PKGS_PREFIX
        defaultValue: ghcr.io/siderolabs
      - name: PKGS
        defaultValue: v1.13.0
---
kind: common.Build
spec:
  ignoredPaths:
    - go.work.sum